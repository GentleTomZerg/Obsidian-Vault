# Obsidian local state — unrelated to wiki content
.obsidian/
.trash/
.DS_Store
*.code-workspace

# Runtime logs — never part of the wiki
logs/

# Sources — never tracked, never published. Books, clippings and dumps all live here.
raw/
